## 前端联调 — 改动总结

### 改动的 8 个文件

| 文件 | 改动 |
|------|------|
| `api/inspire.js` | **完全重写** — 13 个真实 API 调用，覆盖用户/灵感/AI/搜索 |
| `Login.vue` | 调用真实 `login()` API，存 JWT token 到 localStorage |
| `Register.vue` | 新增邮箱字段，调真实 `register()` API |
| `Index.vue` | 子标签点选后从 `/inspire/public/list` 加载真实数据 |
| `Search.vue` | 搜索调 `/search/public`，热门列表调 `/inspire/public/list` |
| `Create.vue` | **新增 AI 生成流程** — 输入关键词 → AI 返回 2 候选 → 选中 → 填写 → 发布 |
| `InspireDetail.vue` | 调真实详情/收藏/点赞 API，精简页面 |
| `Personal.vue` | 调 `/auth/userinfo` + `/inspire/my` 展示真实数据 |
| `InspireCard.vue` | 修复 `heatScore` → `heat`，点击卡片跳转详情页 |

### 启动方式

先确认后端都在跑（开 3 个终端）：

```bash
cd "/Users/lee/Desktop/Project/Study Project/Codex/inspire-ai-preview/backend"
mvn spring-boot:run -pl inspire-gateway
mvn spring-boot:run -pl inspire-auth
mvn spring-boot:run -pl inspire-core
```

再开一个终端启动前端：

```bash
cd "/Users/lee/Desktop/Project/Study Project/Codex/inspire-ai-preview/frontend"
npm run dev
```

浏览器打开 `http://localhost:5173`，现在应该能：
1. 注册（带邮箱）→ 自动登录
2. 首页点分类 → 子标签 → 从后端加载真实灵感列表
3. 创建页面 → 输入关键词点 AI 生成 → 选候选 → 发布到后端
4. 灵感详情 → 真实的收藏/点赞
5. 个人中心 → 显示真实用户信息和发布列表
6. 搜索 → 调 ES 搜索（有 ES 时）或 MySQL 降级

