# 生产性能优化操作清单

> 目标：解决 `ai.20sherry.com` 首屏慢、图片慢、公开接口每次回源的问题。
> 标注「你操作」的步骤需要在 Cloudflare / 服务器后台完成；标注「我操作」的部分可以直接改项目代码。

---

## 一、Cloudflare 控制台（你操作）

进入：

`Cloudflare Dashboard -> 选择 20sherry.com -> Caching -> Cache Rules`

逐条新建规则。建议按下面的顺序创建。

### 0. 按新版“可视化条件构建器”填写

如果你的页面和截图一致，看到的是：

- `URI 完整`
- `通配符`
- `值`
- `And / Or`

就不要填高级表达式，直接按下面这张表建立独立规则。每行建一条规则即可，最简单的填法是：

| 规则 | URI 完整的值 | 边缘 TTL | 浏览器 TTL |
| --- | --- | --- | --- |
| 静态资源 | `https://ai.20sherry.com/assets/*` | 一年 | 一年 |
| 图片接口 | `https://ai.20sherry.com/api/file/view*` | 一个月 | 一个月 |
| 分类接口 | `https://ai.20sherry.com/api/inspire/public/categories*` | 十分钟 | 一分钟 |
| 词云接口 | `https://ai.20sherry.com/api/inspire/public/word-cloud*` | 十分钟 | 一分钟 |
| 推荐接口 | `https://ai.20sherry.com/api/inspire/public/recommend*` | 一分钟 | 一分钟 |
| 首页 HTML | `https://ai.20sherry.com/` | 五分钟 | 遵循源站 |
| index.html | `https://ai.20sherry.com/index.html` | 五分钟 | 遵循源站 |

操作步骤：

1. 规则名称：填表格第一列对应的名字
2. 选择：`自定义筛选表达式`
3. 字段：选择 `URI 完整`
4. 运算符：选择 `通配符`
5. 值：填表格第二列
6. 匹配时动作中设置：
   - 缓存资格：`符合缓存资格 / Eligible for cache`
   - 边缘 TTL：选择表格第三列
   - 浏览器 TTL：选择表格第四列
7. 保存后，再新建下一条规则

> 如果当前页面没有显示“缓存资格 / 边缘 TTL / 浏览器 TTL”，先输入值并点击“编辑表达式”或继续向下滚动，动作设置通常会在条件区域下面。

> 项目使用 Hash 路由，`/#/login`、`/#/personal` 等页面在 HTTP 层仍然请求 `/`，不需要逐个配置页面规则。

### API 缓存必须同步更新 Cloudflare Worker

生产环境 `/api/*` 先经过 `docker/cloudflare/worker.js`，因此 Worker 响应不会自动套普通 Cache Rules。
Worker 已增加 Cache API：

- 分类、词云：缓存 10 分钟
- 推荐、公开列表：缓存 1 分钟
- 带 `Authorization` 的请求不缓存，避免用户个性化数据串缓存
- 响应头增加 `X-Worker-Cache: HIT / MISS`

部署步骤：

1. 打开 `Workers & Pages -> 选择代理 Worker -> Edit Code`
2. 用 `docker/cloudflare/worker.js` 覆盖线上 Worker 代码
3. 点击 `Deploy`
4. 执行 `Purge Everything`

验证：

```bash
curl -sS -D - -o /dev/null \
  'https://ai.20sherry.com/api/inspire/public/categories?workercheck=1'
```

第二次请求期望看到：

```txt
X-Worker-Cache: HIT
```

当前生产已验证：

- 分类：第一次 `MISS`，第二次 `HIT`
- 推荐：第一次 `MISS`，第二次 `HIT`
- 公开列表：可命中 Worker Cache
- 词云：缓存填充后命中 `HIT`

### 3. 隧道、图片 403 与 AI 配图修正

- `start.sh` / `reset-data.sh` 已接入 `docker/cloudflare/start-tunnel.sh`，会检查进程并自动后台启动 cloudflared，不会重复启动。
- Nginx `valid_referers` 已兼容：
  - `127.0.0.1:*`
  - `localhost:*`
  - `*.20sherry.com`
  - `*.github.io`
  - 带路径的完整 Referer
- AI 配图不再让浏览器直连 `images.unsplash.com`，统一返回：
  - `/api/file/poster-cover?url=...`
- `poster-cover` 增加一次重试，降低 Unsplash 偶发连接关闭。
- Worker Cache 增加 `/api/file/poster-cover`，缓存 24 小时。
- AI 配图现在首次会下载并存入 MinIO，之后直接返回 `img.20sherry.com` 地址。
- 现有演示图 URL 已增加 `?v=2`，绕过之前被缓存的旧 403。
- AI 配图结果按“关键词 + 页码”写入 Redis，TTL 24 小时：
  - 第一次测试约 4.07s（下载 6 张图）
  - 第二次同关键词/页码约 0.08s

### 4. 图片真实缩图

发现原配置虽然接收了 `?w=`，但没有真正执行 `image_filter`，因此之前所有 CDN 缩图请求仍返回原图。

现已：

- 加载 `ngx_http_image_filter_module`
- 按 `w` 参数执行等比缩放
- 保留 30 天 Nginx 图片缓存
- 前端缩图 URL 增加 `rs=2`，绕过历史错误缓存

实测：

```txt
原图 ?w 未传：29,652 bytes
?w=100&rs=2：3,187 bytes
```

体积减少约 89%。

### 5. 上传时预生成 WebP 多尺寸

免费方案已实现：

- 上传图片时自动生成：
  - `xxx_w200.webp`
  - `xxx_w400.webp`
  - `xxx_w800.webp`
- 详情图默认使用 `_w800.webp`
- 列表/个人页卡面使用 `_w200.webp`
- 搜索、滑动卡片、AI 配图网格使用 `_w400.webp`
- 旧图片没有 WebP 变体时，继续由 Nginx `image_filter` 动态缩图兜底

实测本地上传：

```txt
_w200.webp  4,156 bytes
_w400.webp 10,606 bytes
_w800.webp 15,950 bytes
```

种子历史图已完成一次性回填：

- 新增 60 × 3 = 180 个 WebP 变体
- 数据库更新 3,000 行图片引用
- 种子图统一改为 `_w800.webp`，前端按场景切换到 `_w200/_w400`

示例 `demo/4`：

```txt
_w200.webp 2,152 bytes
_w400.webp 4,648 bytes
_w800.webp 9,392 bytes
```

### 6. 马善政字体自托管

- 不再请求 `fonts.googleapis.com` / `fonts.gstatic.com`
- 字体转换并放到：

```txt
frontend/public/fonts/ma-shan-zheng.woff2
```

- 只在创建灵感页通过本地 `@font-face` 加载
- `font-display: swap`

**图片规则的缓存键**

图片规则如果打开截图中的 `缓存密钥 / Cache Key` 弹窗：

- 缓存欺骗盔甲：保持关闭
- 按设备类型缓存：保持关闭
- 忽略查询字符串：保持关闭
- 对查询字符串排序：可选，保持关闭即可
- 查询字符串：选择 `所有查询字符串参数`
- 标头：留空，不要添加

也就是使用 Cloudflare 默认缓存键：

`域名 + 路径 + 全部 Query String`

这样 `key=4.jpg&w=400` 和 `key=4.jpg&w=800` 会各自缓存，不会串图。

> 这一项不是必须的。Cloudflare 默认缓存键已经包含完整 Query String，`key` 和 `w` 会自动参与缓存区分。如果当前套餐没有自定义缓存键，保持 `标准 / 默认` 即可，不影响图片缓存生效。

> 如果打开该弹窗后提示需要升级套餐，直接关闭弹窗、不修改即可。不要选择“忽略查询字符串”，否则不同 `key` 或 `w` 的图片可能互相覆盖。对于当前套餐，Cloudflare 的默认行为已经正确。

---

### 1. 哈希静态资源缓存一年

**规则名**

`ai-static-assets-immutable`

**匹配条件**

```txt
http.host eq "ai.20sherry.com" and starts_with(http.request.uri.path, "/assets/")
```

**设置**

- Cache Eligibility：`Eligible for cache`
- Edge TTL：`Override origin`，选择 `1 year`
- Browser TTL：`Override origin`，选择 `1 year`

作用：

- 当前主 JS/CSS 只缓存 4 小时
- Vite 文件名带 hash，内容变化会换文件名，可以放心缓存一年

### 2. 图片接口缓存 30 天

**规则名**

`ai-public-images-cache`

**匹配条件**

```txt
http.host eq "ai.20sherry.com" and starts_with(http.request.uri.path, "/api/file/view")
```

**设置**

- Cache Eligibility：`Eligible for cache`
- Edge TTL：`Override origin`，选择 `30 days`
- Browser TTL：`Override origin`，选择 `30 days`
- Cache Key -> Query String：`Include specific parameters`
- 参数填：`key,w`

作用：

- 实测图片当前是 `cf-cache-status: DYNAMIC`
- 24KB 图片也会回源，TTFB 约 1.5 秒
- 图片 key 不变时内容不变，适合长期缓存

> 注意：调整图片后如果要用同一个 key 覆盖，必须在 Cloudflare 手动 Purge Cache。

### 3. 公开接口短缓存

**规则名**

`ai-public-api-short-cache`

**匹配条件**

```txt
http.host eq "ai.20sherry.com" and (
  http.request.uri.path eq "/api/inspire/public/categories" or
  http.request.uri.path eq "/api/inspire/public/word-cloud" or
  http.request.uri.path eq "/api/inspire/public/recommend"
)
```

**设置**

- Cache Eligibility：`Eligible for cache`
- Edge TTL：分类/词云 `10 minutes`，推荐 `1 minute`
- Browser TTL：`Override origin`，`1 minute`

作用：

- 分类接口实测 TTFB 约 2 秒
- 推荐接口实测总耗时约 2.6 秒
- 这些数据没有用户私有信息，短缓存不会影响登录态

如需把三个接口设置成不同 TTL，就拆成三条规则。

### 4. HTML 首页短缓存

**规则名**

`ai-html-short-cache`

**匹配条件**

```txt
http.host eq "ai.20sherry.com" and (
  http.request.uri.path eq "/" or
  http.request.uri.path eq "/index.html"
)
```

**设置**

- Cache Eligibility：`Eligible for cache`
- Edge TTL：`5 minutes`
- Browser TTL：`Respect origin`

作用：

- 当前 HTML 是 `cf-cache-status: DYNAMIC`
- 每次打开首页都会回源，TTFB 约 1 秒

### 5. 开启 Brotli

进入：

`Speed -> Settings -> 协议优化 / Protocol Optimization`

打开：

- `Brotli`

当前实测：

- 主 JS 使用 Gzip，压缩后 76KB
- 开启 Brotli 后通常还能减少约 10%–20%

### 6. 发布前清理 Cloudflare Cache

每次前端部署完成后：

`Caching -> Configuration -> Purge Cache`

至少清理：

- `/assets/*`
- `/`
- `/index.html`

---

## 二、图片域名检查（你先执行）

项目里已经存在 `img.20sherry.com`，Nginx 配置也已经有：

- `Cache-Control: public, max-age=31536000, immutable`
- `?w=` 缩略图
- 30 天 Nginx 图片缓存

但当前生产演示数据仍然写的是站点代理地址：

```txt
/api/file/view?key=upload/demo/4.jpg
```

所以图片没走已有 CDN。

先在服务器或本机终端测试：

```bash
curl -I 'https://img.20sherry.com/upload/demo/4.jpg?w=400'
```

你要把结果给我：

- 如果返回 `HTTP/2 200`：我可以把公开演示图片切到 `img.20sherry.com`
- 如果返回 `502/403/404`：先修 Cloudflare Tunnel 或 Nginx，否则切过去会整站图片挂掉

### 当前实测结果：502 / 403

本机实测发现三个配置问题：

1. `~/.cloudflared/config.yml` 里 `img.20sherry.com` 指向 `http://localhost:80`，但 `nginx-image` 实际映射端口是 `8088`
2. `docker/minio/nginx/nginx.conf` 把 `/upload/demo/4.jpg` 重写成了 `/inspire-img/demo/4.jpg`，少了 `upload/` 前缀，MinIO 实际对象 key 是 `upload/demo/4.jpg`
3. MinIO 的 `inspire-img` 桶是 `private`，匿名访问返回 403

需要先做：

```bash
# 1. 修改 ~/.cloudflared/config.yml
# img.20sherry.com 的 service 改为：
service: http://localhost:8088

# 2. 重启图片代理
docker compose up -d --force-recreate nginx-image

# 3. 启动 cloudflared 隧道（保持终端常驻）
cloudflared tunnel --config ~/.cloudflared/config.yml run

# 4. 应用 MinIO 桶策略：公开 upload/*，保持 upload/private/* 私有
bash docker/minio/init-public-policy.sh
```

验证：

```bash
curl -I 'http://127.0.0.1:8088/upload/demo/4.jpg?w=400'
curl -I 'https://img.20sherry.com/upload/demo/4.jpg?w=400'
```

两条都应返回 `200`。

`start.sh` 和 `reset-data.sh` 已接入 `docker/minio/init-public-policy.sh`，后续重建容器或清库后会重新应用该策略。

---

## 三、我可以直接改的代码（我操作）

当前进度：

- [x] 演示图片 URL 已切到 `img.20sherry.com`
- [x] 现有 1000 条演示灵感图片已迁移到 CDN
- [x] `DemoDataSeeder` 后续重新生成数据也会使用 CDN
- [x] Google Fonts 已从全局 `index.html` 移到录入页按需加载
- [x] 后端 core / 前端生产构建通过
- [x] 公开接口 Redis 缓存：分类/词云 10 分钟，推荐/公开列表 1 分钟
- [x] 后台修改分类/词云后主动清理 Redis 缓存
- [x] 网关公开接口移除 `Vary`，并补充 `Cache-Control`
- [x] 前端推荐/公开列表增加 60 秒内存缓存
- [ ] 前端首包继续拆分

### 1. Google 字体不再全局阻塞

当前所有页面都会加载：

```html
https://fonts.googleapis.com/css2?family=Ma+Shan+Zheng&display=swap
```

但它实际上只有灵感录入页的词云需要。

我会改成：

- 从全局 `index.html` 移除字体 stylesheet
- 只在进入创建灵感页时异步加载
- 或直接自托管字体文件

收益：

- 避免国内网络下 Google Fonts 慢或超时拖住首屏
- 首页、个人页、搜索页不再请求 Google

### 2. 公开接口增加 Redis 缓存

可以给这些接口增加服务端缓存：

- `/api/inspire/public/categories`
- `/api/inspire/public/word-cloud`
- `/api/inspire/public/recommend`

建议 TTL：

- 分类、词云：10 分钟
- 推荐：30–60 秒

收益：

- 不依赖 Cloudflare 也能减少数据库查询
- 后台修改分类/词云后主动清缓存

### 3. 演示图片切到 CDN

前提：第二节的 `curl` 返回 200。

我会修改：

- `DemoDataSeeder` 里的演示图 URL
- 前端图片工具对 CDN 图片统一追加 `?w=400`
- 列表卡片优先使用缩略图和 WebP

收益：

- 图片不再经 Cloudflare → Gateway → Core → MinIO
- 首张图不再需要约 2.5 秒

### 4. 前端首包继续拆分

可做：

- 首页组件继续按需加载
- 把 Element Plus 的大组件从首页依赖里拆出去
- 进一步减少首页主包和 vendor 包

收益不是第一优先级，前三项做完后再处理。

---

## 四、不要做的缓存配置

不要缓存以下路径：

- `/api/auth/**`
- `/api/message/**`
- `/api/admin/**`
- 带用户 token、私信、个人资料的接口

否则可能出现串号或登录态错误。

---

## 五、优化后的验收命令

### 检查静态资源缓存

```bash
curl -I 'https://ai.20sherry.com/assets/index-D2I-jkdd.js' \
  -H 'Accept-Encoding: br, gzip'
```

期望看到：

- `cf-cache-status: HIT`
- `content-encoding: br`
- `cache-control` 为一年

### 检查图片缓存

```bash
curl -I 'https://ai.20sherry.com/api/file/view?key=upload/demo/4.jpg&w=400'
```

期望看到：

- `cf-cache-status: HIT`
- `cache-control: public, max-age=2592000`

### 检查公开接口

```bash
curl -I 'https://ai.20sherry.com/api/inspire/public/categories'
curl -I 'https://ai.20sherry.com/api/inspire/public/recommend?page=1&size=10'
```

期望：

- 第一次 `MISS`
- 短时间内第二次 `HIT`

### 如果仍然不是 HIT

实测如果看到：

- `/assets/*`：`cf-cache-status: REVALIDATED`，且 `cache-control` 仍是 `max-age=14400`
- `/api/file/view*`：`cf-cache-status: DYNAMIC`
- 公开接口：`cf-cache-status: DYNAMIC`

说明规则只有匹配条件，没有真正执行缓存动作。逐项检查：

1. 规则是否已启用
2. 规则是否建在 `20sherry.com` 这个 Zone
3. 动作里的 `缓存资格` 是否设置为 `符合缓存资格 / Eligible for cache`
4. `边缘 TTL` 是否选择 `覆盖源站 / Override origin`
5. `浏览器 TTL` 是否也选择了覆盖值
6. 规则列表中是否有更靠前的“绕过缓存”规则
7. 修改规则后执行一次 `Purge Cache`

### 404/5xx 浏览器缓存修正

如果 `/assets/*` 规则把 `浏览器 TTL` 覆盖成一年，状态代码 TTL 只会控制 Cloudflare 边缘缓存，浏览器仍可能把 404 缓存一年。

需要在：

`Rules -> Transform Rules -> Modify Response Header`

新版可视化界面里响应状态码不在普通字段下拉框中，点击“编辑表达式”后直接填写：

新建：

**匹配条件**

```txt
starts_with(http.request.uri.path, "/assets/") and
http.response.code in {404 500 502 503 504}
```

**动作**

设置静态响应头：

```txt
Cache-Control: public, max-age=10
```

作用：正常 JS/CSS 仍保留一年缓存；资源不存在或服务异常时，浏览器只缓存 10 秒。

动作区选择：

```txt
修改响应头 -> 选择项目 -> 设置静态
标头名称：Cache-Control
值：public, max-age=10
```

---

## 六、建议执行顺序

1. Cloudflare：静态资源一年缓存
2. Cloudflare：图片接口 30 天缓存
3. Cloudflare：公开接口短缓存
4. Cloudflare：开启 Brotli
5. 你测试 `img.20sherry.com`
6. 我改字体加载方式
7. 我改演示图片 CDN 地址
8. 我给公开接口加 Redis 缓存
9. 最后再优化前端首包

---

## 七、下一阶段：真正解决本机隧道延迟

当前最大瓶颈已经不是 Redis 或前端，而是：

```txt
Cloudflare Edge -> Cloudflare Tunnel -> 家庭 Mac -> Docker -> MySQL
```

Worker Cache 只解决重复请求；每次缓存未命中仍然要绕回家庭网络。可选方案按性价比排序：

1. **后端迁到云服务器**
   - 香港/新加坡/东京小规格 VPS 即可
   - Gateway/Core/Redis 在云端，cloudflared 只从云服务器出隧道
   - 首字节延迟会从 1–2 秒降到几百毫秒

2. **图片迁到 R2 或 Cloudflare Images**
   - 不再依赖 `img.20sherry.com -> 家庭 Nginx -> MinIO`
   - 取消图片上的 Cloudflare Tunnel 往返
   - 支持全球边缘缓存和图片变体

3. **公开数据放 Cloudflare KV**
   - 分类、词云、推荐和公开列表可写入 KV
   - Worker 直接读 KV，不再回源
   - 后台修改后由管理接口更新 KV

4. **前端迁到 Cloudflare Pages**
   - 比 GitHub Pages 更容易控制缓存、预览和清理
   - 原生 Brotli、HTTP/3
   - 与 Worker、R2、KV 的发布链路更统一

5. **前端首包继续拆分**
   - Element Plus 组件继续按页面拆分
   - 首页、创建页按路由拆包
   - 非首屏组件延迟加载
