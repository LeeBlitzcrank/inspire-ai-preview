本文件为项目全局强制顶层开发规范，无具体技术代码约束，聚焦架构思维、开发原则、性能标准、扩展准则、产品规范。所有AI辅助开发、功能迭代、架构优化、问题修复、代码重构，必须严格遵守本规范。
项目核心定位：极致高性能、高可扩展、低维护成本、可商业化落地、符合企业级开发标准的全栈web项目。

---
一、核心开发总原则（全局最高优先级）
1. 性能极致优先：所有功能开发、迭代、重构，性能优先级高于功能实现，禁止为快速开发牺牲长期性能，杜绝隐性性能损耗。
2. 架构解耦优先：所有模块、业务、逻辑、层级完全解耦，单一职责，互不侵入，新增功能不改动核心底层代码。
3. 高扩展可插拔：所有业务功能、配置、规则、组件支持扩展，无硬编码逻辑，支持后续迭代新增业务、切换配置、拓展场景。
4. 分层绝对规范：前后端所有层级边界清晰，各司其职，禁止跨层调用、逻辑混乱、职责混杂。
5. 缓存前置兜底：所有查询类业务优先走缓存，底层数据源仅做最终兜底，减少核心服务压力。
6. 瓶颈前置规避：提前预判网络、服务、渲染、并发各类瓶颈，从架构层面提前规避，不依赖后期补救优化。
7. 稳定性优先：任何迭代更新，优先保障项目稳定可用，杜绝崩溃、卡顿、报错、空白、数据异常等问题。

---
二、通用架构规范（无技术绑定）
1. 分层架构准则
   整体架构分为：边缘网络层、静态资源层、业务网关层、应用服务层、缓存层、数据持久层，每层职责唯一、独立运行、可单独优化升级，互不影响其他层级。
   所有流量、请求、资源、数据严格分层流转，禁止跳过层级、跨层级混用、层级职责重叠。
2. 流量分流准则
   静态资源、媒体资源、业务接口、个性化接口、公开接口、私有接口，全部流量隔离、域名隔离、缓存策略隔离，避免资源挤占、流量干扰、互相影响性能。
3. 数据处理准则
   区分静态固定数据、低频变更数据、高频动态数据、个性化私有数据，针对不同数据类型，配置专属缓存、更新、查询、存储策略，不统一粗暴处理。
   所有数据查询按需返回，杜绝冗余数据、无效数据、超大数据包传输。

---
三、性能硬性规范（全局通用）
1. 网络性能规范
- 最大限度减少外网回源请求、减少跨网传输流量、减少请求次数、减小数据包体积。
- 公开静态资源、公开只读接口，优先边缘缓存，实现就近响应，规避长链路网络延迟。
- 杜绝重复请求、无效请求、空请求、冗余请求，所有请求具备去重、节流、防抖能力。
- 缓存策略兼顾响应速度与数据一致性，优先保障用户访问速度，容忍极短时间数据延迟更新。
2. 服务性能规范
- 核心业务链路极简，剥离所有非核心、非必要逻辑，保证主链路高效响应。
- 所有耗时、非核心、统计类逻辑异步处理，不阻塞主请求、不占用核心服务资源。
- 服务资源合理分配，杜绝资源抢占、线程阻塞、连接耗尽、内存溢出等问题。
- 高频访问数据预计算、预缓存，避免用户访问时实时计算造成延迟。
3. 前端渲染性能规范
- 杜绝DOM节点无限堆积，长列表、海量数据仅渲染可视区域，保证页面滚动、交互丝滑。
- 所有资源按需加载、懒加载、分片加载，首屏资源最小化，非首屏资源延迟加载。
- 页面无白屏、无闪烁、无卡顿、无阻塞，所有交互有状态反馈、异常有兜底。
- 静态资源极致压缩、精简，杜绝冗余代码、冗余资源、超大静态包。

---
四、高扩展迭代规范
1. 功能扩展准则
- 所有功能模块化、组件化、配置化，支持一键开启/关闭、参数配置修改，无需改动核心代码。
- 预留业务拓展接口、拓展字段、拓展路由，支持后续新增业务场景，无需重构架构。
- 功能迭代遵循「新增不修改、迭代不破坏」原则，保证旧功能稳定，新功能独立拓展。
2. 架构扩展准则
- 整体架构支持横向扩容、功能插拔、模块新增、策略替换，适配后续流量增长、业务新增。
- 所有规则、策略、参数外置配置，不硬编码，支持动态调整。
- 兼容多端适配、多场景使用、多业务复用，组件与逻辑高复用、低耦合。

---
五、安全与稳定性规范
- 区分公开资源与私有资源，严格权限隔离，私有数据禁止公开访问、缓存泄露。
- 所有异常场景做兜底处理，避免页面崩溃、接口报错、数据丢失、功能失效。
- 防范各类缓存异常、请求异常、并发异常，保障高访问场景稳定性。
- 严格区分可缓存资源与不可缓存资源，杜绝脏缓存、错误缓存、数据错乱问题。

---
六、用户体验通用规范
- 优先优化用户感知速度，弱化网络延迟、加载等待带来的负面体验。
- 所有加载、请求、操作状态可视化，无空白、无静默等待。
- 适配全终端设备，保证不同设备、不同网络环境下的统一体验。
- 交互简洁流畅，逻辑清晰，容错性高，避免用户误操作、无效操作。

---
七、AI开发强制约束
所有AI生成的代码、功能、优化方案，必须先满足本顶层规范，再落地具体技术实现。禁止出现以下情况：
- 禁止为实现功能牺牲性能、解耦性、扩展性
- 禁止编写硬编码、高耦合、难维护、不可扩展的逻辑
- 禁止忽略缓存策略、流量隔离、分层规范
- 禁止生成冗余代码、冗余请求、冗余资源
- 禁止破坏现有高性能架构，所有迭代兼容原有最优规范

---
八、提交前检查策略
默认采用快速提交模式，优先保证开发迭代速度，禁止每次提交前机械执行全量构建、全量测试和重复验证：
- 文档、注释、纯样式改动：只检查变更文件，不执行全量构建。
- 前端小范围改动：按需构建或依赖本地热更新验证一次，不重复执行相同验证。
- 后端小范围改动：只编译受影响模块，不默认构建整个后端。
- 已通过本地运行或页面验证的同一轮改动：提交前不再重复执行全量验证。
- 用户明确要求直接提交时：不再额外追加全量代码检查。

以下高风险改动仍须执行最小必要的针对性验证：
- 数据库结构、分片规则、索引、缓存键、迁移脚本。
- 登录鉴权、Token、权限、会话隔离。
- 文件上传、视频处理、存储和部署链路。
- 公共接口契约、跨模块数据结构或影响多个页面的共享逻辑。
